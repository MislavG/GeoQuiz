@model IEnumerable<GeoQuiz.Models.HighScore>
@{
    ViewBag.Title = "GeoQuiz";
}
<body class="container body-content">
    <div class="jumbotron">
        <h1>Geo-Quiz</h1>
        <p class="lead">Educational application about geography</p>
    </div>

    <div class="row">
        <div class="col-md-4">
            <h2>Info</h2>
            <p>
                This application is a project of FER students and it's made for education and entertainment. :)
            </p>
        </div>
        <div class="col-md-4">
            <h2>Top 5 highscores</h2>
            <p>
                @*@foreach (var item in Model)
                {
                    <li>
                            @Html.DisplayFor(modelItem => item.Nick)
                            @Html.DisplayFor(modelItem => item.Score)
                        </li> *@
                        @*<td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.SifraHS }) |
                            @Html.ActionLink("Details", "Details", new { id = item.SifraHS }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.SifraHS })
                        </td>
                }
                @*<li>Tofi : 150</li>
                <li>Martina : 100</li>
                <li>Mic : 30 </li>
                <li>Ana : 20</li>
                <li>Antonio : 10</li>*@
                <script type="text/javascript">
    $.ajaxSetup({
        // Disable caching of AJAX responses
        cache: false,
        error: function (jqXHR, exception) {
            if (jqXHR.status === 0) {
                alert('Not connect.\n Verify Network.');
            } else if (jqXHR.status == 404) {
                alert('Requested page not found. [404]');
            } else if (exception === 'parsererror') {
                alert('Requested JSON parse failed.');
            } else if (exception === 'timeout') {
                alert('Time out error.');
            } else if (exception === 'abort') {
                alert('Ajax request aborted.');
            } else if (jqXHR.status == 500) {
                alert('Internal Server Error [500].');
            } else {
                alert('Uncaught Error.\n' + jqXHR.responseText);
            }
        }
    });

    var MyAppUrlSettings4 = {
        MyUsefulUrl4: '@Url.Action("DohvatiHS","Home")'
    }
                    function DohvatiHS() {
                        var abc = $.ajax({
                            type: "POST",
                            url: MyAppUrlSettings4.MyUsefulUrl4,
                            data: {},
                            async: false,
                            error: function (msg) {
                                callback(msg);
                            },
                            success: function (msg) {
                                brojkontinenata = msg;
                                $("#divResult").html("success");
                            },
                        });
                    }

                    DohvatiHS();
    </script>

            </p>
        </div>
        <div id="earth" style="float:left"></div>
        <style>
            #earth {
                width: 200px;
                height: 200px;
                background: url(http://www.noirextreme.com/digital/Earth-Color4096.jpg);
                border-radius: 100%;
                background-size: 420px;
                box-shadow: inset 16px 0 40px 6px rgb(0, 0, 0), inset -3px 0 6px 2px rgba(255, 255, 255, 0.2);
                animation-name: rotate;
                animation-duration: 4s;
                animation-iteration-count: infinite;
                animation-timing-function: linear;
            }
        </style>
        @*@keyframes rotate {
                from {
                    background-position-x: 0px;
                }

                to {
                    background-position-x: 210px;
                }
            }*@

    </div>
</body>